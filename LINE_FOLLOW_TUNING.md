# 循迹现场检查

烧录后先架空车轮，不按启动键，左右移动黑线并观察 OLED：

1. `S:xxxxxxxx` 必须跟随八路传感器灯变化。
2. 中间两路亮时应为 `S:00011000 E +0`。
3. 黑线向右移动时，误差应为正；向左移动时应为负。
4. 按启动后，`TA`、`TB` 应随误差反向变化，不应始终相等。
5. `EA`、`EB` 是归一化到每 50 ms 的编码器实测计数。

若第 1 项不变化，应检查 OUT/PA14、AD0/PB18、AD1/PB19、AD2/PB10 和
传感器地线。若第 1 项变化但左右方向相反，可将
`BSP/inc/bsp_line_follow.h` 中的 `LINE_SENSOR_REVERSED` 改为 1。
